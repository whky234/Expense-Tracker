Place your branding images here:

- `app_logo.png` (1024x1024 recommended, PNG)
- `splash_logo.png` (512x512 or 1024x1024 PNG with transparent background)

These files are used by:

- `flutter_launcher_icons` for app icon generation
- `flutter_native_splash` for native splash screen generation
